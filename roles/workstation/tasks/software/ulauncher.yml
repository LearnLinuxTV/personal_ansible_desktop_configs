- name: software | ulauncher | install ppa
  tags: ulauncher
  apt_repository:
    repo: 'ppa:agornostal/ulauncher'
    state: present
  when:
    - ansible_distribution in ["Pop!_OS", "Ubuntu"]
    - ulauncher is defined
    - ulauncher == true

- name: software | ulauncher | install package
  tags: ulauncher
  apt:
    name: ulauncher
    state: latest
  when:
    - ansible_distribution in ["Pop!_OS", "Ubuntu"]
    - ulauncher is defined
    - ulauncher == true

- name: software | ulauncher | enable autostart
  tags: ulauncher
  copy:
    src: users/andro/ulauncher.desktop
    dest: /home/andro/.config/autostart/ulauncher.desktop
    owner: andro
    group: andro
    mode: 0600
  when:
    - ansible_distribution in ["Pop!_OS", "Ubuntu"]
    - ulauncher is defined
    - ulauncher == true
